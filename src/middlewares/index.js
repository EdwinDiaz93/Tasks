const validateToken = require('./validateToken');
const validatorRequest = require('./validatorRequest');
const validateRols = require('./validateRols');


module.exports = {
    validateToken,
    validatorRequest,
    validateRols
}